# Optional public map config (never commit real secrets).
# Copy to .env.local for local builds, or set in CI for Pages deploy.
#
# VITE_CARTO_API_KEY=
#
# Isolated Yandex Maps v3 prototype (sea-map/yandex-proto/):
# VITE_YANDEX_MAPS_API_KEY=
# Restrict the key by HTTP Referer (e.g. http://localhost:* for local Vite).
